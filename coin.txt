  if (command === 'coin') {
    axios.get(`https://customapi.aidenwallis.co.uk/api/v1/misc/coinflip`)
      .then((response) => {
        let flipresult = response.data
        if (flipresult === 'Heads') {
          client.say(channel, `${tags.username} Result of your coin flip: "Heads!" (Yes)`);
        }
        else {
          client.say(channel, `${tags.username} Result of your coin flip: "Tails!" (No)`);
        }
      });
  }